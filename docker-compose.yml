version: '3.7'
services:

  mongodb:
    image: mongo:5.0.14
    container_name: luckysix-mongo
    environment:
      - MONGO_DATA_DIR=/data/db
      - MONGO_LOG_DIR=/dev/null
      - MONGODB_DATABASE=luckysix
      - MONGO_INITDB_ROOT_USERNAME=admin
      - MONGO_INITDB_ROOT_PASSWORD=toor
    volumes:
      - ./.data/db:/data/db
    ports:
      - 27019:27017

  postgres:    
    image: postgres:15
    container_name: luckysix-postgres
    volumes:
      - pg-data:/var/lib/postgresql/data
    environment:
      POSTGRES_USER: 'luckysix'
      POSTGRES_PASSWORD: 'luckysix'
      POSTGRES_DB: 'luckysix'
    ports:
      - 5439:5432

  rabbitmq:    
    image: rabbitmq:3.11.4-management
    container_name: luckysix-rabbitmq
    ports:
      - 5679:5672
      - 15679:15672

  redis:
    image: 'bitnami/redis:7.0.7'
    environment:
      - ALLOW_EMPTY_PASSWORD=yes
      - REDIS_PORT_NUMBER=7000
    container_name: luckysix-redis
    ports:
      - "7009:7000"

  app:
    image: app
    container_name: luckysix-app
    build: .
    environment:
      PG_URL: 'postgres://luckysix:luckysix@luckysix-postgres:5432/luckysix'
      RMQ_URL: 'amqp://guest:guest@luckysix-rabbitmq:5672/'
    ports:
      - 8089:8080
    depends_on:
      - postgres
      - rabbitmq

  integration:
    build:
      context: .
      dockerfile: integration-test/Dockerfile
    container_name: luckysix-integration
    image: integration
    depends_on:
      - app

volumes:
  pg-data:
